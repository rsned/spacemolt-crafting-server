# DEPRECATED: This workflow has been migrated to /home/robert/spacemolt/kb/
# See DEPRECATED.md for details. This workflow will be removed in a future version.
name: Deploy Catalog

on:
  push:
    branches: [main]
    paths: [catalog/**]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: catalog/
      - id: deployment
        uses: actions/deploy-pages@v4
